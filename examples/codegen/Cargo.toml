[package]
name = "example-codegen"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
conflaguration = { path = "../..", features = ["derive"] }

[build-dependencies]
conflaguration = { path = "../..", features = ["derive", "codegen"] }
